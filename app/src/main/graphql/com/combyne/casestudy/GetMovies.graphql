query getMovies($order:[MovieOrder!]){
    movies(order: $order) {
        edges {
            node {
                title
                releaseDate
                seasons
            }
        }
        count
    }
}